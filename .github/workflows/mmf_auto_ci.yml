name: MMF Auto CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt || true

      - name: Run Lint
        run: |
          echo "No lint script yet"  # 추후 flake8 등 적용

      - name: Run Tests
        run: |
          echo "No test script yet"  # tests/ 폴더 연동시 교체

      - name: Auto Sync PatchNotes
        run: |
          echo "Patch note auto-sync stub"
